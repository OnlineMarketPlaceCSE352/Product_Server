services:
  product-db:
    image: mariadb
    environment:
      MARIADB_ROOT_PASSWORD: rootpass
      MARIADB_DATABASE: product_db
    ports:
      - "3308:3306"
    volumes:
      - product-data:/var/lib/mysql

volumes:
  product-data: